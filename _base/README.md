# `_base/` — shared theme and toolchain defaults

**Placeholder.** In a later milestone this directory will hold the shared theme and toolchain configuration (`myst.yml` defaults) that every product inherits, so that look, feel and build behaviour are defined once and reused across all published series and courses.

Nothing here is wired up yet. See the repository [README](../README.md) for the current state and roadmap.
